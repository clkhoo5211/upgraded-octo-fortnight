# Vercel环境变量设置指南

## ✅ 设置位置确认

**是的，这是正确的设置位置！**

Vercel Dashboard → Settings → Environment Variables

## 📋 设置步骤

### 1. 基本设置

- **Key**: `GITHUB_TOKEN`
- **Value**: 你的GitHub Personal Access Token
- **Environments**: 选择 "All Environments"（推荐）

### 2. 安全设置（推荐）

**启用 "Sensitive" 选项**：
- ✅ 将 "Sensitive" 开关切换到 **"Enabled"**
- ✅ 原因：Token是敏感信息，启用后团队成员无法查看
- ✅ 更安全：即使有访问权限也无法看到token值

### 3. 保存

- 点击 **"Save"** 按钮
- ⚠️ **重要**：保存后需要重新部署才能生效

## 🔍 Token格式确认

### Fine-grained Token（推荐）
- 格式：`github_pat_...`（以`github_pat_`开头）
- ✅ 你当前使用的就是这个格式
- ✅ 完全支持，无需修改代码

### Classic Token
- 格式：`ghp_...`（以`ghp_`开头）
- ✅ 也完全支持

## ⚠️ 保存后操作

### 1. 重新部署

保存环境变量后，Vercel会提示需要重新部署：
- 可以手动触发重新部署
- 或等待下次代码推送自动部署

### 2. 验证设置

测试归档功能：

```bash
curl -X POST https://upgraded-octo-fortnight.vercel.app/api/archive \
  -H "Content-Type: application/json" \
  -d '{
    "max_results": 2,
    "download_content": true,
    "save_to_github": true
  }'
```

**成功标志**：
```json
{
  "success": true,
  "saved_files": [
    "2025/11/12/tech.md",
    "2025/11/12/finance.md"
  ]
}
```

**失败标志**：
```json
{
  "success": false,
  "errors": [
    {
      "error": "保存到GitHub失败: ..."
    }
  ]
}
```

## 🔒 安全建议

1. ✅ **启用Sensitive选项**：保护token不被查看
2. ✅ **使用Fine-grained Token**：限制到特定仓库
3. ✅ **设置过期时间**：定期更新token
4. ✅ **不要分享token**：token就像密码

## 📝 检查清单

- [ ] Key设置为 `GITHUB_TOKEN`
- [ ] Value填入GitHub token
- [ ] 启用 "Sensitive" 选项（推荐）
- [ ] 选择环境范围（All Environments）
- [ ] 点击 "Save" 保存
- [ ] 重新部署应用
- [ ] 测试归档功能

## 🎯 总结

✅ **设置位置正确**
✅ **Token格式正确**（Fine-grained token）
✅ **建议启用Sensitive选项**
✅ **保存后需要重新部署**

保存后，归档功能就可以正常工作了！

