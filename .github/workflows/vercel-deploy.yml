name: Vercel 部署

on:
  workflow_dispatch:  # 仅手动触发，Vercel会自动通过GitHub集成部署

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: 显示部署信息
        run: |
          echo "ℹ️ Vercel部署说明"
          echo "================================"
          echo ""
          echo "✅ Vercel已通过GitHub集成自动部署"
          echo ""
          echo "📋 部署方式："
          echo "1. Vercel会自动检测GitHub推送并部署"
          echo "2. 无需手动触发或配置Token"
          echo "3. 推送代码到main分支会自动部署"
          echo ""
          echo "🔍 查看部署状态："
          echo "- Vercel Dashboard: https://vercel.com/dashboard"
          echo "- 项目: upgraded-octo-fortnight"
          echo ""
          echo "📝 如果需要手动重新部署："
          echo "- 在Vercel Dashboard中点击 'Redeploy'"
          echo "- 或推送新的commit到main分支"
          echo ""
          echo "================================"
